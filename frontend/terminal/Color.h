#pragma once

namespace frontend::terminal {

/** Color of character. */
enum Color {
  /** Black. */
  kBlack = 0,
  /** Red. */
  kRed = 1,
  /** Green. */
  kGreen = 2,
  /** Yellow. */
  kYellow = 3,
  /** Blue. */
  kBlue = 4,
  /** Magenta. */
  kMagenta = 5,
  /** Cyan. */
  kCyan = 6,
  /** White. */
  kWhite = 7
};

}
