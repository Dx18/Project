#pragma once

namespace frontend {

/** Pressed key. */
enum Key {
  /** Down arrow key. */
  kDown,
  /** Up arrow key. */
  kUp,
  /** Left arrow key. */
  kLeft,
  /** Right arrow key. */
  kRight,
  /** Home key. */
  kHome,
  /** End key. */
  kEnd,
  /** Page up key. */
  kPageUp,
  /** Page down key. */
  kPageDown,
  /** Backspace key. */
  kBackspace,
  /** Delete key. */
  kDelete,
  /** Tab key. */
  kTab,
  /** Back tab key. */
  kBackTab,
  /** Enter key. */
  kEnter,
  /** Escape key. */
  kEscape,
  /** F1 key. */
  kF1,
  /** F2 key. */
  kF2,
  /** F3 key. */
  kF3,
  /** F4 key. */
  kF4,
  /** F5 key. */
  kF5,
  /** F6 key. */
  kF6,
  /** F7 key. */
  kF7,
  /** F8 key. */
  kF8,
  /** F9 key. */
  kF9,
  /** F10 key. */
  kF10,
  /** F11 key. */
  kF11,
  /** F12 key. */
  kF12
};

}
